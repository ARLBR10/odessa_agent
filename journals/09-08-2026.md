# 2026-08-09 TRY28 Close-Range Build And Hardware Result

## Build Artifact

- Artifact: `lineage-23.2-20260809-TRY28-UNOFFICIAL-odessa.zip`.
- OTA SHA-256:
  `db2283135abbd5192047a05e469ec09911bb51c561b4d82214143257821a97a4`.
- Size: 1,028,380,603 bytes. OTA metadata timestamp and incremental:
  `1786236578`; device `odessa`; Android SDK 36; security patch 2026-07-01.
- The build initially created the named file as a 17-link hardlink to mutable
  `lineage_odessa-ota.zip`. It was replaced with a byte-identical Btrfs reflink;
  the preserved TRY28 path has link count 1 and retained the SHA-256 above.
- `unzip -t` passes. Build VINTF log reports `COMPATIBLE`.

## Exact Payload Verification

- Extracted the OTA payload's seven A/B partitions: `boot`, `dtbo`, `product`,
  `recovery`, `system`, `vbmeta`, and `vendor`.
- `boot`, `dtbo`, `recovery`, `vbmeta`, and `vendor` match target-files images
  byte-for-byte. Sparse target-files `product` and `system` also match after
  conversion to raw images with the build's `simg2img`.
- Exact payload boot SHA-256:
  `291bbe43854a2288c274fe03fd56cf82cbdf5399747742b58463502cc3867b58`.
- The kernel unpacked from that boot image matches the built `Image`
  byte-for-byte; SHA-256:
  `a37970ddae94870bfba07112594d6fefa9680f6647b5282f95842303a523bb37`.
- Kernel version is `4.14.357-openela-perf+`. `System.map` and `vmlinux` contain
  `sys_close_range`. Both arm64 native `sys.o` and compat `sys32.o` relocate
  `sys_close_range` at byte offset `0xda0`; `0xda0 / 8 = 436`, proving syscall
  table entry 436 is built for both ABIs.

## Status

- With explicit user authorization, the exact TRY28 OTA was sideloaded through
  Lineage Recovery from running slot B to target slot A. Host transfer completed
  at `1.00x`, and Recovery reported installation complete.
- Automatic first boot looped. The user flashed `gpt.bin` from the validated
  RPAS31.Q2-59-17-4-3-9 stock package; TRY28 then booted successfully. Read-only
  ADB proved slot A, timestamp/incremental `1786236578`, boot completion, kernel
  `4.14.357-openela-perf+`, and SELinux enforcing. This is another
  `ODESSA-004` reproduction; the OTA images themselves are bootable.

## Hotspot Hardware Result

- PASS: the TRY28 hotspot toggle remains enabled.
- PASS: a second device connected, resolved DNS names, and loaded Internet
  content through the hotspot. No SSID, credential, address, or client identifier
  was retained.
- Runtime while tethering showed both `dnsmasq` and `ipacm` alive and
  `dumpsys tethering` responsive within the five-second bound.
- Current-boot logs contained zero occurrences of netd's prior
  `Failed to send update command to dnsmasq` broken-pipe error and zero spawn
  exit-127 errors.
- This hardware-resolves `ODESSA-008`: TRY27's manifest 1.1 change fixed the
  offload-HAL deadlock, and TRY28's native/compat syscall 436 backport fixed the
  subsequently exposed `dnsmasq` spawn failure.
