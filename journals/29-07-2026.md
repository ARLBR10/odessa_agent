# Journal 29-07-2026

## Device-tree checkout recovery

- `lunch lineage_odessa-bp4a-userdebug` failed because
  `device/motorola/sm6150-common/BoardConfigCommon.mk` included the obsolete
  `device/qcom/sepolicy_vndr-legacy-um/SEPolicy.mk` path. Roomservice then emitted
  misleading follow-on messages about Odessa being unavailable on lineage-23.2.
- Both `device/motorola/odessa` and `device/motorola/sm6150-common` had been freshly
  checked out at 18:25 in detached HEAD state from the historical commits pinned in
  `manifests/odessa.xml`. Their local object databases did not contain the documented
  bring-up commits.
- The commits were recoverable from public fork branches:
  `ARLBR10/android_device_motorola_odessa` lineage-23.2 at
  `7784600c5caf6fea26e3f888e73bff4c632ced34`, and
  `ARLBR10/android_device_motorola_sm6150-common` lineage-23.2 at
  `f64cb3e53f497b3c6af7041a5913a13c25d619b2`.
- Added each fork as the local Git remote `fork`, fetched it, and restored each
  checkout to a local `lineage-23.2` branch tracking `fork/lineage-23.2`.
- Updated `manifests/odessa.xml` to use the two `ARLBR10` project names and immutable
  commits above. The active `.repo/local_manifests/odessa.xml` remains a symlink to
  this tracked manifest. The kernel pin changes already present in the outer
  worktree were not modified as part of this recovery.
- Verification: the exact command
  `source build/envsetup.sh && lunch lineage_odessa-bp4a-userdebug` succeeded and
  reported LineageOS 23.2, Android 16, build ID `BP4A.251205.006`, target
  `lineage_odessa`, and `userdebug`. No image build and no device operation ran.

## Shared vendor restoration

- Restored the Android 16 Widevine compatibility output in
  `vendor/motorola/sm6150-common`: the tracked `vendor/lib64/libwvhidl.so` now
  has `libcrypto_shim.so` in `DT_NEEDED`, matching the committed extraction
  fixup in `device/motorola/sm6150-common/extract-files.py`. Its generated
  `Android.bp` module now depends on AOSP `hardware/lineage/compat:libcrypto_shim`.
  No `allow_undefined_symbols` workaround was added.
- Removed the obsolete wifilearner binary, HAL prebuilt, and init rc from the
  shared vendor payload. Their existing generated `Android.bp` and
  `sm6150-common-vendor.mk` removals were retained.
- Verification: `llvm-readelf --dynamic` reports `libcrypto_shim.so` and
  `libcrypto.so`; `rg` finds no wifilearner reference; `xmllint --noout` across
  tracked XML files and `git diff --check` both pass. No Android build or device
  operation ran.

## Kernel rollback and recurring post-OTA partition failure

- User hardware result: the completed BPF backport bootloops before Recovery.
  Kernel work is paused; the tested build uses OpenELA-only commit
  `70aea3b5f901b3298685f2736fef899afa8711d8` (4.14.357). Kernel source was not
  changed.
- Corrected `manifests/odessa.xml` from BPF-backport commit `295fa6418564` to
  `70aea3b5f901`, and removed `ro.bpf.kver_override=5.10.239` from Odessa while
  that non-backported kernel is selected. The override falsely advertises BPF
  features and must not accompany this kernel. Android 16 is consequently still
  expected to stop at netbpfload; this kernel selection is for Recovery and
  boot-control diagnosis, not a bootable-OS claim.
- User hardware result after sideloading the latest A/B OTA: neither Android nor
  Recovery boots from the newly selected slot. Restoring the stock GPT makes the
  pre-existing Lineage Recovery boot and `Start` launch stock Android 11. The
  exact GPT changes have not yet been captured, so "partition-table corruption"
  remains a symptom description rather than a proven mechanism.
- Source checkout is at bootctrl `6024c108fc43` and common tree `f64cb3e53f49`.
  `out/soong/build.lineage_odessa.incremental.ninja` shows
  `1.1/libboot_control_qti/libboot_control_qti.cpp` in the recovery service and
  `-DXBL_SLOT_BY_GPT_ATTRIBUTES` on both the recovery service source and static
  `libgptutils.qti`. The current host recovery service SHA-256 is
  `0b04514baa21fe093c85a4ee7c105eda90ef6ea03ee899dde5cf9677525d1783`.
  Both dated OTA names are hardlinks/content-identical; SHA-256 is
  `7a4611cd5d74ff71e2d2597d7d1515a69899a2b7cd203b7328349f9bfa69b051`.
- After the user made Recovery ADB available, the running recovery service hash
  exactly matched the host value above. It reports slot A and kernel
  `4.14.357-openela-perf+`; the normal A/B block-device map is visible after the
  stock GPT restore. This proves the intended bootctrl binary is now running,
  although no surviving artifact independently proves which binary ran during
  the already-completed sideload.
- Slot B contains the exact packaged target-files images: `boot_b` SHA-256
  `7cadbdff37a8889b44c3436c3177412a0cebcd03ad988299f2def039c37677bc`
  and `recovery_b` SHA-256
  `6a0a8fa29613fd1f32f8ed4ba3aa0f5b9891fc5d0b2f6ee2dd1b3749fa9fa8cd`.
  The separately generated/slot-A recovery has a different whole-image hash only
  because AVB regenerated its unsigned hash descriptor with a different salt;
  unpacking proves the two images have byte-identical kernel, ramdisk, DTB, and
  recovery-DTBO. Stale or truncated OTA boot/recovery payloads are ruled out.
- Read-only hashes of the safe boot-chain pairs `xbl`, `xbl_config`, `abl`, `aop`,
  `cmnlib`, `cmnlib64`, `devcfg`, `hyp`, `keymaster`, `qupfw`, `tz`, and
  `uefisecapp` are identical between slots A and B. A stale slot-B firmware chain
  is ruled out for those components.
- The stock GPT restore erased the failed install's decisive attributes, and no
  recovery `last_log`/`last_install` survived. The next diagnostic must capture
  GPT attributes immediately before sideload and after status 0 but before reboot.
  No flash, erase, format, sideload, slot switch, or other device write was issued
  during this investigation.

## Motorola A/B attribute encoding fix

- A controlled before/after capture of the next successful sideload disproved
  partition-table corruption and the earlier split-XBL regression. All primary
  and backup GPT CRCs remained valid, all A/B partitions switched together, and
  the UFS boot LUN remained pinned to 2. The failing bootctrl encoded slot B as
  Qualcomm `0x3f` active and slot A as `0x40` inactive/successful.
- Recovery B, DTBO B, vbmeta B, the safe A/B firmware pairs, and the OTA payload
  were all hash-verified. Recovery B's kernel, ramdisk, DTB, and recovery-DTBO
  were byte-identical to the working slot-A image. A temporary `fastboot boot`
  of that image also failed while the Qualcomm attributes selected B, exhausting
  B's retries without degrading the bootloader's two-slot partition view.
- Motorola MBM's measured working `fastboot set_active` operation uses exactly
  `0x04` for every active partition and `0x00` for every inactive partition.
  Updated both copies of bootctrl's `UPDATE_SLOT` macro so only devices setting
  `XBL_SLOT_BY_GPT_ATTRIBUTES` use that exact encoding; upstream Qualcomm
  behavior remains unchanged for other devices.
- The user built and flashed the candidate Recovery. The running AIDL service
  matched the host SHA-256
  `3fce8792d7d58dc6bf31b33ada922b4cb0867aa8a2f68ca862fe5771cab56522`;
  both recovery partitions matched the new image, and the kernel remained
  `4.14.357-openela-perf+` from `70aea3b5f901`.
- A second sideload completed with status 0. Direct before/after measurement
  showed every slot-A partition become exactly `0x00` and every slot-B partition
  become exactly `0x04`, including `xbl`, `xbl_config`, `multiimgoem`, and
  `multiimgqti`; primary and backup copies agreed. `adb reboot recovery` then
  booted Lineage Recovery successfully from slot B, hardware-verifying the fix.
- Raw GPT captures and the recovery log were kept only under `/tmp/opencode`, per
  user instruction; none were saved in the workspace. The two bootctrl source
  files were committed and pushed as `6a85678788e1`; the temporary non-BPF
  Odessa property correction was pushed as `6518db3a8131`. The manifest now pins
  both immutable revisions.

## BPF recovery isolation resumed

- Reconstructed the unresolved hardware range. `df9da243d122` remains the exact
  proven-good commit; the next commit, `37e035f8c918`, was never tested alone and
  does not produce a complete kernel. The intermediate `eb0458ea6a0f` also does
  not build; `8c13ed9d15ab` is the first later commit that links the full kernel.
- Re-fetched the current Xiaomi SM6150 `lineage-23.2` reference at
  `e03e5ba22586`. The completed Motorola backport has the same unconditional
  XDP RX-queue registration in `net/core/dev.c` as the reference, but that path
  is absent at the known-good Odessa boundary and runs whenever any network
  device is allocated, before Recovery starts.
- Created local branch `wip/bpf-backport-xdp-rxq-isolation` from the completed
  backport `295fa6418564`. Its sole worktree change replaces
  `xdp_rxq_info_reg()` with `xdp_rxq_info_unused()` during net-device RX-queue
  allocation. This is a one-variable recovery diagnostic, not a final XDP fix.
  `xdp_rxq_info_unreg()` explicitly permits the unused state; `git diff --check`
  passes. No build or device write has run yet.
- Hardware result: the XDP RX-queue isolation recovery built successfully but
  still bootlooped before Recovery. Unconditional XDP RX-queue registration is
  therefore not the sole cause. Restored `net/core/dev.c` exactly to
  `295fa6418564`, renamed the local branch to
  `wip/bpf-backport-bpf-events-isolation`, and moved to the next one-variable
  diagnostic: disable `CONFIG_BPF_EVENTS` while retaining the BPF syscall/core,
  networking, and JIT.
- Hardware result: the BPF-events-disabled recovery built successfully but also
  bootlooped before Recovery. BPF tracing/perf-event support is therefore not
  the sole cause. Replaced that checkpoint with a broader subsystem split:
  disable `CGROUP_BPF`, `BPF_SYSCALL`, `BPF_JIT_ALWAYS_ON`, `NET_CLS_BPF`, and
  `BPF_JIT`, while retaining classic `CONFIG_BPF=y` and the imported networking
  core. This distinguishes the eBPF core/JIT from the networking/core backport;
  it is diagnostic only and cannot satisfy Android's BPF loader. The local
  branch is now `wip/bpf-backport-legacy-bpf-isolation`; its only source delta
  from `295fa6418564` is the seven defconfig settings above (including the
  already-tested `BPF_EVENTS` disable).
- The first legacy-BPF build failed in `kernel/bpf/core.c`, not in the networking
  split: disabling `BPF_JIT_ALWAYS_ON` compiled the interpreter for the first
  time and exposed a missing `__annotate_jump_table` macro. Imported the exact
  `CONFIG_STACK_VALIDATION`/fallback definitions from the Xiaomi reference into
  `include/linux/compiler.h`. This is a real completeness fix hidden by the
  production JIT-only configuration; it does not alter that production path.
- The legacy-BPF kernel then linked successfully. Normal `m recoveryimage`
  stopped only at the expected VINTF check because Android kernel FCM level 6
  requires `BPF_JIT`, `BPF_SYSCALL`, `CGROUP_BPF`, and `NET_CLS_BPF`. Used the
  existing `m recoveryimage-nodeps` target to package this diagnostic without
  changing product VINTF policy. Recovery SHA-256 is
  `3a9fff47ceedd90779274fd8b66b17d82f506b8864dcd86de7e7c5f30f746dbe`;
  its extracted kernel exactly matches the built `Image`, SHA-256
  `e331b60260e2267de7f50f2088fe2e811ee78c1479bc8d8aaa8d358483cd5866`.
- After explicit user approval for `recovery_b` only, read-only fastboot checks
  showed slot B active and the device connected. `fastboot flash recovery_b`
  completed successfully (send and write both `OKAY`). No slot switch, erase,
  format, or reboot was issued.
- Hardware result: the legacy-BPF diagnostic booted Recovery successfully from
  slot B. This rules out the imported 5.x networking/core implementation and
  unconditional XDP RX-queue registration as sufficient causes. The failure is
  inside the disabled group: eBPF syscall/core, cgroup/classifier integration,
  or ARM64 JIT.
- Prepared the next split by restoring `BPF_SYSCALL`, `CGROUP_BPF`,
  `NET_CLS_BPF`, and `BPF_UNPRIV_DEFAULT_OFF`, while retaining
  `BPF_JIT=n`, `BPF_JIT_ALWAYS_ON=n`, and `BPF_EVENTS=n`. This runs full eBPF on
  the interpreter and will distinguish the eBPF core/verifier from the JIT. The
  local branch is `wip/bpf-backport-interpreter-isolation`; relative to
  `295fa6418564`, only those three defconfig disables and the required
  `__annotate_jump_table` interpreter compile fix differ.
- The first interpreter-only build exposed a second no-JIT compile gap:
  `verifier.c` calls `bpf_jit_blinding_enabled()` unconditionally, but
  `filter.h` provided it only under `CONFIG_BPF_JIT`. Added a false-returning
  stub in the existing no-JIT section. It cannot affect JIT-enabled builds.
- The interpreter-only kernel then linked. VINTF rejected exactly one missing
  requirement, `CONFIG_BPF_JIT`, as expected. Built `Image` SHA-256 is
  `24236fa97064953894d1b4640ca9951c32117d92bfbcae32c263b091bbbdfb1c`.
  Generated `.config` has `BPF_SYSCALL`, `CGROUP_BPF`, `NET_CLS_BPF`, and
  `BPF_EVENTS` enabled, with `BPF_JIT` disabled. `BPF_EVENTS` is a promptless
  `default y` symbol, so the attempted defconfig disable could not take effect
  once its dependencies were enabled; restored that defconfig line. The test is
  therefore a clean full-eBPF-core versus JIT split.
- Packaged the interpreter-only kernel with `m recoveryimage-nodeps`. Recovery
  SHA-256 is
  `56a5585ce8842a5d352d289e325395905ea7d35665e553dc961b04b069caffdd`;
  its extracted kernel exactly matches `Image` SHA-256
  `24236fa97064953894d1b4640ca9951c32117d92bfbcae32c263b091bbbdfb1c`.
  After a new explicit approval for `recovery_b` only, fastboot confirmed slot B
  active and the flash completed with send/write both `OKAY`. No erase, slot
  switch, format, or reboot was issued.
- Hardware result: the full-eBPF interpreter-only image booted Recovery from
  slot B. This cleanly isolates the bootloop to JIT execution; the eBPF core,
  verifier, cgroup/classifier integration, BPF events, and imported networking
  core all boot without it.
- The completed backport's `arch/arm64/net/bpf_jit_comp.c` is byte-identical to
  Xiaomi reference `e03e5ba22586`; this is not a simple missed hunk in that file.
  Prepared the next checkpoint with `CONFIG_BPF_JIT=y` but
  `CONFIG_BPF_JIT_ALWAYS_ON=n`. The JIT code and dedicated region are present,
  but execution defaults off and the interpreter remains available. This tests
  static JIT integration separately from actual JIT compilation/execution.
- The JIT-compiled/default-off kernel linked. VINTF rejected only
  `CONFIG_BPF_JIT_ALWAYS_ON`, so `bacon` cannot complete for this intentional
  diagnostic and product policy was not weakened. The concurrently generated
  Recovery is valid: SHA-256
  `b78a55c49e68bf7b9c25f8d3fff466d4b38f97db071730f64bc3bcb8024a24f9`;
  its extracted kernel exactly matches built `Image` SHA-256
  `6c452debaf579801ec0b4f85946a8ce7d3dae5e790d57bcfccba0db8f6ffe05e`.
  Generated config has the full eBPF/JIT/events set except
  `BPF_JIT_ALWAYS_ON=n`.
- After a fresh explicit approval for `recovery_b` only, fastboot confirmed slot
  B active and flashed the verified JIT-default-off Recovery; send and write
  both returned `OKAY`. No erase, slot switch, format, or reboot was issued.
- Hardware result: the JIT-compiled/default-off image booted Recovery. The JIT
  source and dedicated virtual region are therefore safe while unused; the loop
  requires `BPF_JIT_ALWAYS_ON` behavior.
- Root-cause candidate confirmed against Linux v5.10 and Xiaomi: the backport's
  `kernel/bpf/core.c` initializes `bpf_jit_enable` from
  `CONFIG_BPF_JIT_DEFAULT_ON`, but Odessa omitted that symbol,
  `ARCH_WANT_DEFAULT_BPF_JIT`, and ARM64's select. Thus `bpf_jit_enable` stayed
  zero even with `BPF_JIT_ALWAYS_ON=y`, while that option simultaneously compiled
  out the interpreter. Imported the exact Kconfig definitions/select, removed an
  accidental duplicate `BPF_UNPRIV_DEFAULT_OFF` block, and restored the production
  `BPF_JIT_ALWAYS_ON=y` defconfig. Expected result: `bpf_jit_enable` initializes
  to one before program allocation and every program receives a valid JIT runtime.
- Production-config build succeeded. Generated `.config` contains
  `ARCH_WANT_DEFAULT_BPF_JIT=y`, `BPF_JIT_DEFAULT_ON=y`,
  `BPF_JIT_ALWAYS_ON=y`, and the full required BPF set. GDB reads the initialized
  `vmlinux` data symbol `bpf_jit_enable` as exactly `1` (it is in the data section,
  not zero-initialized BSS), directly verifying the fix. Recovery SHA-256 is
  `95e5854c2dcacf2d59bf0340e646c2076a9453600d7bfdda188dbc2914e14cbd`;
  its extracted kernel exactly matches built `Image` SHA-256
  `abc5f1281d2388e370e5676b41984079dd4fb49af366eb55ac0c6b7b27ebef75`.
- After explicit approval for `recovery_b` only, a transient USB re-enumeration
  made the first parallel `fastboot devices` check empty; the slot query waited
  and returned B, and a repeated connectivity check succeeded. The approved
  `recovery_b` flash then completed with send/write both `OKAY`. No erase, slot
  switch, format, or reboot was issued.
- **Hardware result: root fix PASS.** The production-config Recovery booted from
  slot B without looping. This proves the missing `BPF_JIT_DEFAULT_ON` /
  `ARCH_WANT_DEFAULT_BPF_JIT` integration was the pre-Recovery bootloop root
  cause: `BPF_JIT_ALWAYS_ON` had removed the interpreter while the absent default
  symbol initialized `bpf_jit_enable` to zero. Restoring the exact 5.10 Kconfig
  chain initializes it to one and gives programs a valid JIT runtime.
- Restored Odessa's `ro.bpf.kver_override=5.10.239` for the next local full-ROM
  test now that the matching production-config backport boots hardware. Replaced
  the stale OpenELA-only rollback comment with the feature/provenance warning.
  The manifest remains pinned to OpenELA-only `70aea3b5f901` because it must not
  point at an uncommitted kernel worktree; Repo must not be synced before the
  kernel fix is committed and the immutable pin is updated.
