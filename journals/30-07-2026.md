# 2026-07-30

## BPF root-fix full build verification

- `m -j8 bacon recoveryimage` completed successfully in 7m40s with the
  hardware-tested BPF JIT default fix and Odessa
  `ro.bpf.kver_override=5.10.239`.
- VINTF reports `COMPATIBLE`. Generated kernel config contains
  `ARCH_WANT_DEFAULT_BPF_JIT=y`, `BPF_JIT_DEFAULT_ON=y`,
  `BPF_JIT_ALWAYS_ON=y`, `BPF_JIT=y`, `BPF_SYSCALL=y`, `CGROUP_BPF=y`,
  `NET_CLS_BPF=y`, and `BPF_EVENTS=y`. GDB reads initialized
  `bpf_jit_enable` as exactly `1` from `vmlinux`.
- OTA:
  `lineage-23.2-20260730-UNOFFICIAL-odessa.zip`, 1,028,108,519 bytes,
  SHA-256 `7fdc68d6e6503b7ca10a40fddbe55ff401b27cfaf84901eaa417e9b68db17a83`.
  The `TRY5` filename is a hardlink to the same inode/content.
- The packaged vendor build property contains exactly
  `ro.bpf.kver_override=5.10.239`. Loose, target-files boot, and target-files
  recovery images all embed kernel SHA-256
  `abc5f1281d2388e370e5676b41984079dd4fb49af366eb55ac0c6b7b27ebef75`,
  the same kernel that booted Recovery with production BPF config.
- Created a temporary target-files ZIP under `/tmp/opencode` and used the
  checkout's native `brillo_update_payload verify`/`delta_generator` path to
  apply the exact OTA `payload.bin`. All 2,265 operations completed; generated
  `boot`, `dtbo`, `product`, `recovery`, `system`, `vbmeta`, and `vendor`
  partitions matched their target images byte-for-byte. The regular-file ioctl
  and checkpoint warnings are expected in host-side application. Payload
  operation/partition hashes were verified; payload signature validation was
  skipped because no public key was supplied for this test-key build.
- No OTA install, sideload, slot switch, erase, format, or other device write
  was issued during full-build verification.
- With explicit user approval to publish, committed and pushed the JIT/Kconfig
  fix as kernel `56146fa516106938ed3a8f0c0e187f3c679371a4` and the matching
  Odessa BPF property as `fc7495d15985fb611be1e8287f8efb2c58e48fa7`.
  Updated `manifests/odessa.xml` to pin both immutable revisions, replacing the
  temporary OpenELA-only kernel and property-rollback pins.

## First full OTA sideload and new reboot blocker

- Running Recovery B was verified as Odessa/XT2087-1 on
  `4.14.357-openela-perf+`. The user explicitly approved sideloading the verified
  OTA SHA-256 `7fdc68d6e6503b7ca10a40fddbe55ff401b27cfaf84901eaa417e9b68db17a83`.
- Sideload completed with Recovery status 0. Update engine explicitly selected
  target slot A, verified all partition hashes, completed postinstall, and wrote
  `Update successfully applied, waiting to reboot`.
- Before reboot, `boot_a`, `recovery_a`, and `dtbo_a` hashes exactly matched the
  OTA target images. The running Recovery lacked the `bootctl` CLI, so selected
  GPT attributes were not independently queried through it.
- The user explicitly approved `adb reboot recovery` to test Recovery A before
  Android. The command returned no captured output and was user-aborted during
  reboot. The user reports a very fast reboot cycle and suspects the earlier
  partition-table/slot-attribute problem has returned.
- No post-reboot evidence was captured. GPT damage is unproven; current selected
  slot, attribute encoding, CRC status, and boot stage are unknown. All device
  interaction stopped at the user's request. Standalone handoff:
  `docs/bpf-backport-jit-root-cause-20260730.md`.
