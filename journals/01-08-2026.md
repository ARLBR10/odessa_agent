# 2026-08-01

## Read-only Recovery inspection

- ADB confirmed the phone is in slot-A Lineage Recovery 23.2 on
  `4.14.357-openela-perf+`. No flash, erase, format, sideload, reboot, or
  partition write was issued.
- Recovery reports `ro.bpf.kver_override=5.10.239`. This confirms the corrected
  system-property declaration is visible in the currently installed slot-A
  Recovery environment; it does not prove that the stalled Android boot passed
  NetBpfLoad.
- None of the temporary boot-stage files exists under `/metadata/vold`. The
  extended first-stage diagnostic remains unbuilt/uninstalled, so marker absence
  does not locate the current failure.
- `/metadata/bootstat` contains none of the queried boot-reason or boot-complete
  records. The generic current `ro.boot.bootreason=reboot` does not identify the
  prior Android stall.
- Current `/tmp/recovery.log` only covers this Recovery boot and contains no
  slot fallback, update-engine, AVB, BPF, or prior Android failure. The raw log
  is retained only under `/tmp/opencode` because it contains device identifiers.
- Recovery cannot mount `/data` directly and a dry-run F2FS check of the raw
  `userdata` block reports no plaintext F2FS superblock. This is expected for
  the already-verified metadata-encryption design and is not new corruption
  evidence; do not format or repair based on this result.

## Conclusion

- The BPF-backported kernel continues to boot Recovery. There is no new evidence
  that the ARM64 JIT fix regressed or that the current Android stall is in BPF.
- No retained channel currently distinguishes a first-stage mount failure from
  later init work. The smallest justified next experiment is the existing
  extended marker diagnostic in `system/core` and `system/sepolicy`, followed by
  exact artifact verification and an approved install with GPT capture before
  and after installation.
- No additional source diagnostic was added during this inspection. Existing
  temporary diagnostic changes pass `git diff --check` in `system/core`,
  `system/sepolicy`, and `device/motorola/odessa`.

## Extended diagnostic build and pre-install verification

- The user completed `m -j8 init init_first_stage host_init_verifier` and
  `m -j8 bacon recoveryimage` successfully.
- Installable A/B OTA:
  `lineage-23.2-20260801-UNOFFICIAL-odessa.zip`, 1,028,096,263 bytes,
  SHA-256 `53b76f045cf4c5b0ff73722655a796e5f7ad2473856d75ac6ed618e3b145a674`.
  `lineage_odessa-ota.zip` is a hardlink to the same inode and content.
- The packaged target-files boot and system init binaries contain every direct
  marker string through metadata mount, logical-device creation, system and
  remaining first-stage mounts, SELinux setup, second-stage entry, and boot
  script loading. Packaged `SYSTEM/etc/init/hw/init.rc` contains the fsynced
  post-fs-data, BPF start/done, and zygote-start actions.
- Packaged platform SELinux policy grants init the temporary
  `vold_metadata_file` directory and file permissions required by the markers.
  `ro.bpf.kver_override=5.10.239` appears in `SYSTEM/build.prop` and is absent
  from all packaged vendor build properties.
- Native `ota_extractor` successfully applied the payload operations and
  extracted all seven target partitions. Payload-extracted `boot`, `dtbo`,
  `product`, `recovery`, `system`, `vbmeta`, and `vendor` match target-files
  byte-for-byte after sparse-to-raw conversion for product and system.
- Key exact payload/target image SHA-256 values:
  `boot` `ec174e3626241b9c628a287b42e91a80c2a6326dad138c36709e422fac8ecf29`;
  `dtbo` `8d80708752b4bcc2170e2c7adcb8b41c39d7d19ca38cc1586616dc9982c87293`;
  `recovery` `e8d5ad92ebe7e1d89e0215c885d7191fc0cc4c11bf0f40fdfe9552699e39e6f0`;
  `vbmeta` `c0ea26fdc9bcd9e2a684fbac75c2b6c52b6cae3f68bd632351e3101f181b0615`.
- A read-only pre-install GPT capture is retained only at
  `/tmp/opencode/gptcap-pre-20260801-firststage`. Every captured primary and
  backup header and entry-array CRC passes. Slot A consistently uses Motorola's
  active value `0x04`, slot B is `0x00`, and `boot_a=0x37` carries the normal
  attempt state. No device partition was written during build verification or
  this capture.

## Extended diagnostic installation

- The user explicitly approved sideloading the exact verified OTA. Running
  Recovery was slot A, so update_engine selected target slot B.
- Sideload completed with Recovery status 0. Update engine verified all seven
  target partition hashes, completed postinstall, and logged
  `Update successfully applied, waiting to reboot`.
- Read-only hashes of installed `boot_b`, `recovery_b`, and `dtbo_b`, plus the
  8,192-byte vbmeta payload in `vbmeta_b`, match the exact OTA target hashes.
- The immediate post-install GPT capture is retained only at
  `/tmp/opencode/gptcap-post-20260801-firststage`. All primary and backup GPT
  CRCs pass. The only pre/post differences are the intended complete slot
  switch: every A boot-chain partition changed to `0x00` and every B boot-chain
  partition, including XBL, changed to Motorola's active value `0x04`.
- This installation directly disproves persistent GPT corruption for this
  reproduction. No partition-table restore is needed before the diagnostic
  Android boot.
- Raw install log:
  `/tmp/opencode/recovery-20260801-firststage-install.log`; it remains untracked
  because it contains device-specific information.

## Controlled slot-B boot and stage result

- The first post-install reboot entered fastboot. The user independently flashed
  the stock partition table, which restored the GPT byte-for-byte to the
  pre-install A-active capture. The Android stall after that restore therefore
  ran old slot A and could not test the slot-B diagnostic.
- With explicit approval, Motorola MBM's `fastboot set_active b` selected B.
  The exact installed slot-B Recovery then booted successfully, proving the B
  boot chain and diagnostic Recovery image are valid without another install.
- A controlled Android B reboot exposed Motorola Android USB after 48 seconds
  but did not expose ADB during a three-minute trace. The user returned directly
  to Recovery B without restoring GPT.
- Important correction: Recovery does not automatically mount `/metadata`.
  Earlier absent-marker checks examined Recovery's empty ramdisk directory and
  were not evidence. Mounting the metadata partition strictly read-only with
  `ro,noload` exposed the retained markers.
- Hardware result: the diagnostic boot reached `boot-scripts-loaded`,
  `post-fs-data`, `bpf-start`, `bpf-done`, and `zygote-start`. This proves the
  full BPF loader completed on the backported kernel. BPF is no longer the boot
  blocker.
- `zygote-start` was recorded immediately before
  `wait_for_prop odsign.verification.done 1`. The next diagnostic adds markers
  after that wait and after the statsd, primary zygote, and secondary zygote
  start commands. Only `system/core/rootdir/init.rc` changed for this extension.

## TRY12 result and bootctrl correction

- TRY12 OTA `lineage-23.2-20260801-TRY12-UNOFFICIAL-odessa.zip` is
  1,028,096,491 bytes with SHA-256
  `d0b4c5151c0c01d14b4098fa6a3f1338b9bf022de97edbb3204de2c36b283e33`.
  ZIP integrity passes, all new marker actions are packaged, and native payload
  extraction reproduces all seven target-files partitions byte-for-byte.
- With explicit approval, TRY12 installed from B to A with status 0. Every
  partition hash and postinstall passed. The post-install GPT had valid CRCs and
  switched the entire boot chain to A, but encoded `boot_a=0x04`.
- The direct Recovery-A reboot entered fastboot. The user again restored stock
  GPT and ran `fastboot set_active a`; exact TRY12 Recovery A then booted.
- The post-`set_active a` GPT differs from the rejected post-OTA GPT only at two
  attribute bytes: `boot_a 0x04→0x3f` and `boot_b 0x00→0x02`. All other GPT
  structures and A/B attributes are identical. Together with the earlier B
  direction (`boot_a 0x37→0x32`, `boot_b 0x04→0x3f`), this proves Motorola uses
  hybrid encoding: boot carries priority/attempt state while the rest of the
  chain uses `0x04`/`0x00`.
- Updated both Odessa-gated bootctrl `UPDATE_SLOT` copies so target boot becomes
  `0x3f`, inactive boot keeps its high attempt bits and gets low value `0x02`,
  and all non-boot partitions remain `0x04`/`0x00`. The source change is
  unbuilt, uncommitted, and not yet under hardware test.
- A controlled TRY12 Android-A boot again stalled after exposing Motorola
  Android USB. Read-only metadata markers prove odsign verification completed
  and init issued starts for statsd, primary zygote, and secondary zygote.
- The next RC-only diagnostic records `early-boot`, `boot`, boot completion, and
  running/restarting states for zygote, secondary zygote, SurfaceFlinger, and
  boot animation.

## TRY13 result

- TRY13 OTA `lineage-23.2-20260801-TRY13-UNOFFICIAL-odessa.zip` is
  1,028,091,018 bytes with SHA-256
  `a1ba141efe324c6452b49687619fcfbb47934140492e5311d0accb2a760b5e6d`.
  ZIP integrity, all service-state markers, and byte-identical payload versus
  target-files checks pass for all seven partitions.
- With explicit approval, exact TRY13 `recovery_a` and matching `vbmeta_a` were
  flashed to bootstrap the new bootctrl. Running Recovery A's boot-control
  service hash exactly matched packaged SHA-256
  `9568eda10b1854b14290005f8e8b3a3ee58d576e2e4087c548c1c78065b1aa6f`.
- TRY13 installed A-to-B with status 0 and complete partition verification.
  Bootctrl correctly produced target `boot_b=0x3f` and all non-boot B attributes
  `0x04`, but preserved the marked-successful old boot as `boot_a=0x72`.
- The direct Recovery-B attempt was not stable. The user restored stock GPT and
  ran `fastboot set_active b`; exact TRY13 Recovery B then booted. Comparing this
  working table to post-OTA changes exactly one byte: `boot_a 0x72→0x3a`.
  Therefore the required boot encoding is fixed `0x3f` active / `0x3a` inactive,
  not preservation of high bits. Both bootctrl copies now implement that result;
  the correction is unbuilt and uncommitted.
- A controlled TRY13 Android B boot reached `early-boot` and `boot`. Boot
  animation reached running. Primary zygote, secondary zygote, and SurfaceFlinger
  each reached running and then restarting; boot completion remained absent.
  The stall is a userspace crash/restart loop.
- The next unbuilt diagnostic enables stock bounded `logcatd` output under
  encrypted `/data/misc/logd` and copies only its current log to
  `/metadata/vold/odessa-bootdiag-logcat` from primary zygote's existing
  `onrestart` hook. Init receives read-only access to `misc_logd_file`; no USB,
  permissive mode, broad denial suppression, or logging relocation is added.

## TRY14 result and GPU root cause

- TRY14 OTA `lineage-23.2-20260802-TRY14-UNOFFICIAL-odessa.zip` has SHA-256
  `06d8029b837ba700c1c3befdff2f3031360dae517be01e5cbc6156b201e0d2d0`.
- The corrected bootctrl implementation produced exact active/inactive boot
  attributes `0x3f`/`0x3a`, retained `0x04`/`0x00` for every other A/B
  partition, and booted target Recovery A directly after a status-0 B-to-A OTA.
  This hardware-verifies the final Odessa GPT encoding without a GPT restore or
  `fastboot set_active`.
- Metadata journal replay was separately authorized to recover the bounded
  diagnostic log. `/tmp/opencode/odessa-bootdiag-logcat-try14` is 1,945,600 bytes
  with SHA-256
  `3f00471955313d59b52c2d76c1de9cbad4f4e8bbf87a21288cc9dd82a3ce6750`.
- The log changes the diagnosis from a generic SurfaceFlinger/EGL failure to a
  kernel firmware-loading failure. SurfaceFlinger's render thread invokes the
  Adreno driver; `adreno_start()` fails, `/dev/kgsl-3d0` returns `EAGAIN`, EGL
  maps that to `EGL_BAD_ALLOC`, and SurfaceFlinger aborts because it cannot
  initialize any EGLConfig.
- The initiating kernel errors are `_request_firmware_load` failing for
  `a615_zap.mdt` and PIL reporting `Failed to locate a615_zap.mdt(rc:-11)`.
  GMU fence/lowest-idle warnings and the GX GBIF halt timeout occur in the failed
  start cleanup path and are downstream symptoms.
- A read-only `ro,noload` mount of installed `modem_a` proved the complete ZAP
  image exists under `image/`. No partition was written. The extracted generic
  signed files and SHA-256 values are:
  - `a615_zap.b00`: `ee96dc28c5ff4259f50fbcd068389de56a8f233a1f150976a101fed5d1d279fe`
  - `a615_zap.b01`: `b11113d5f9190c2dc70fd30268943cef61a6f47eeeeecd6ab7b5fd62526de0bd`
  - `a615_zap.b02`: `61be42f041eb6051eb9f56beb1f98ed97ab21faf2ef2df6b9151ba1a46aae878`
  - `a615_zap.mdt`: `d380504e3467d6ae5352fb0e44cc90cd8a2e2ad8d5c6a6e3d8f4a714e88ea55d`
- Added those four paths to SM6150 common `proprietary-files.txt`, copied them to
  the private shared vendor repository, and regenerated its copy rules so the
  next vendor image places them in `/vendor/firmware`. This first-stage-mounted
  location is already in ueventd's standard firmware search path. The change is
  unbuilt, uncommitted, and requires exact package verification before a device
  test.

## TRY15 artifact verification

- TRY15 `lineage-23.2-20260802-TRY15-UNOFFICIAL-odessa.zip` is 1,028,075,215
  bytes with SHA-256
  `964ce112d29afd929aa93a9ada15afe0946b13cc52b5ffe55ddd2cfc67d391aa`.
  `lineage_odessa-ota.zip` is the same size and hash. ZIP integrity passes.
- Native `ota_extractor` completed all seven payload partitions. Payload boot,
  DTBO, recovery, vbmeta, and vendor match target-files byte-for-byte. Payload
  product and system match target-files byte-for-byte after converting the
  target sparse images to raw. Exact payload SHA-256 values are:
  - `boot`: `ec174e3626241b9c628a287b42e91a80c2a6326dad138c36709e422fac8ecf29`
  - `dtbo`: `8d80708752b4bcc2170e2c7adcb8b41c39d7d19ca38cc1586616dc9982c87293`
  - `product`: `0ccdafe97f699ebd6713b9c5212cc65c62fcd3b0873d5f7c48a36c48d7d2804f`
  - `recovery`: `0e787adf0129ab4e3dd0050d8bf01f1e5b94a6ce286c3a5607780d35620fefee`
  - `system`: `8700a58ab8142ec276e277b93881f8f8bd7a2bc21762cfacd1c434f4981c9c19`
  - `vbmeta`: `baeccc53c0d574de792a41070102f2e06b1d08d50bb186edf00bdae616f30722`
  - `vendor`: `132c3267c1457c98a7333943ea90a927e40499407147ac284144ead626dac7ba`
- The payload-extracted vendor filesystem contains all four `a615_zap` files.
  Their content hashes exactly match the read-only modem source and each inode
  carries `u:object_r:vendor_firmware_file:s0`.
- Read-only readiness check: the phone remains in exact TRY14 Lineage Recovery
  on slot A, battery reports 100%, and metadata is unmounted. TRY15 has not been
  installed and no device partition was written during verification.

## TRY15 installation

- The user explicitly approved sideloading exact TRY15 from Recovery A to the
  seven slot-B targets. At the user's direction, installation proceeded without
  a new pre-install GPT capture because the phone was already in sideload mode.
- Recovery completed with status 0. Update engine selected source A/target B,
  applied all 2,265 operations, verified the payload hash and exact target hash
  for every partition, completed postinstall, and logged `Update successfully
  applied, waiting to reboot`.
- The immediate post-install GPT capture has valid primary/backup header and
  entry-array CRCs everywhere. It contains exact intended attributes:
  `boot_a=0x3a`, `boot_b=0x3f`, every other A partition `0x00`, and every other
  B partition `0x04`.
- The direct reboot-to-Recovery observation timed out on the host. Before its
  result could be established, the user manually ran
  `fastboot flash partition downloads/ODESSA_RETAIL_RPAS31.Q2_59_17_4_3_9_subsidy_DEFAULT_regulatory_DEFAULT_CFC.xml/gpt.bin`,
  then exact TRY15 Recovery B was observed (`ro.boot.slot_suffix=_b`, build
  incremental `1785697348`).
- A capture after that stock GPT flash is byte-for-byte identical to the
  immediate post-OTA capture across all ten primary/backup GPT regions. The
  manual flash therefore made no GPT attribute or structural correction. It
  invalidates a clean automatic-Recovery observation but does not show that GPT
  content caused the timeout.

## TRY15 Android result and memtrack blocker

- TRY15 Android B exposed Motorola Android USB after 49 seconds and displayed
  the animated Lineage three-dot boot animation, but no ADB or boot completion
  appeared during four minutes. The user returned directly to Recovery B
  without another GPT flash or `set_active`.
- The only GPT change across the Android attempt was normal boot attempt state:
  `boot_b 0x3f→0x37`; every primary/backup CRC remains valid and all other bytes
  are unchanged.
- The fresh bounded log is 1,437,696 bytes with SHA-256
  `22595aa859ae0e132b13763c03124a143a95d77c9d0ea18bc64d8eb4e2f7a8dc`.
  It contains no `a615_zap`, Adreno open, EGL, GMU, or GX failure. This
  hardware-verifies that packaging the four ZAP files fixed the TRY14 GPU
  blocker.
- The new blocker is exact: SystemServer calls `startMemtrackProxyService()`,
  waits for declared HIDL `android.hardware.memtrack@1.0::IMemtrack/default`,
  and repeatedly starts the passthrough service. That service logs
  `hw_get_module memtrack failed: -2` because no legacy
  `memtrack.<hardware>.so` is packaged. After 66 seconds SystemServer watchdog
  kills the process while its main thread remains in the native memtrack proxy
  start call.
- Upstream LineageOS Xiaomi SM6150 commit
  `5f487d934999372a73cab757714e5c1212358a5c` fixed this exact integration in
  2024 by replacing HIDL memtrack with QTI's AIDL service. Applied the analogous
  Motorola common change: remove `android.hardware.memtrack@1.0-impl` and
  `android.hardware.memtrack@1.0-service`, remove the HIDL device-manifest entry,
  and package `vendor.qti.hardware.memtrack-service`. The module supplies its own
  init RC and AIDL VINTF fragment. The change passes `git diff --check` but is
  unbuilt and uncommitted.

## TRY16 artifact verification

- TRY16 `lineage-23.2-20260802-TRY16-UNOFFICIAL-odessa.zip` is 1,028,026,086
  bytes with SHA-256
  `d9fd140ebb1971962802e985c39ec823feeac432b8c061cbcce545e678b2617d`.
  `lineage_odessa-ota.zip` has the same size and hash; ZIP integrity passes.
- Native payload extraction completed all seven partitions. Payload boot, DTBO,
  recovery, vbmeta, and vendor match target-files directly; product and system
  match after sparse-to-raw conversion. Payload SHA-256 values are:
  - `boot`: `ec174e3626241b9c628a287b42e91a80c2a6326dad138c36709e422fac8ecf29`
  - `dtbo`: `8d80708752b4bcc2170e2c7adcb8b41c39d7d19ca38cc1586616dc9982c87293`
  - `product`: `012f66cb8f537181a2c182c526219f65cc02af4ead4575eba2586dcbd1e65f29`
  - `recovery`: `9b6cd7b4d4c00e55a70715dd672a63a87b6490e302623709362897a1ef937664`
  - `system`: `7c24a7aea5d7eaf1a4b377a4aa84341495fb3547e1b3d0ebfb64e100d3ae375d`
  - `vbmeta`: `c166f0b5905bc3a0505416da97d60b04de714fda004f53548df826a37d46adb9`
  - `vendor`: `a169c5a178bf980611e845f1e4d51d8e0aa4eb8bd7b29905f351cfa4b24b137b`
- Payload vendor contains QTI AIDL memtrack binary
  `vendor.qti.hardware.memtrack-service` (SHA-256
  `3142c5dd50984312be974fe34054e06937eb80efdc63d459ffefabb2d69846ec`),
  its init RC (`dacbe63a...`) and AIDL VINTF fragment (`47343d26...`). The old
  `android.hardware.memtrack@1.0-service` executable and HIDL device declaration
  are absent. All four `a615_zap` firmware hashes remain exact.
- TRY16 is artifact-verified but not installed.

## TRY16 installation and Android result

- The user explicitly approved exact TRY16 from Recovery B to all seven slot-A
  targets. Sideload completed with status 0; update engine selected target A,
  verified every installed partition against the exact payload hash, completed
  postinstall, and logged update success.
- Immediate post-install GPT CRCs all pass and attributes were exact:
  `boot_a=0x3f`, `boot_b=0x3a`, every other A partition `0x04`, and every other
  B partition `0x00`.
- Automatic slot selection was cleanly hardware-proven before any manual GPT
  action: exact TRY16 Recovery A booted directly (`_a`, incremental
  `1785701278`) without GPT restore or `fastboot set_active`.
- TRY16 Android reached Motorola Android USB after 49 seconds. The user observed
  a Lineage horizon animation, black screen, return to the three-dot animation,
  and another black screen, indicating framework restart cycles.
- After that observation the user again manually flashed stock `gpt.bin` and ran
  `fastboot set_active a`. The resulting capture has valid CRCs but, as expected,
  no longer preserves the original Android-attempt state: all A non-boot slot
  attributes are `0x44`, `boot_a=0x77`, and `boot_b=0x02`.
- Fresh bounded TRY16 log is 765,952 bytes with SHA-256
  `7f6646f966d5d8074e068d5a3983c1cc912e2a613d32dfee67ae9bf6d2807a53`.
  The TRY15 memtrack wait/failure is absent, proving QTI AIDL memtrack works.
  SystemServer reaches `bootAnimationComplete` and final screen enable, then its
  display thread blocks 60 seconds in
  `LineageHardwareManager.getAIDLService()` at the
  `FEATURE_DISPLAY_MODES` case (`LineageHardwareManager.java:292`). Watchdog
  kills SystemServer, producing the observed animation/black restart cycle.
- Payload declares `vendor.lineage.livedisplay.IDisplayModes/default`, but the
  SDM service never registers it. Added the branch-current capability setting
  `$(call soong_config_set_bool,livedisplay_sdm,enable_dm,false)` next to the
  service package. This removes unsupported Display Modes code/VINTF declaration
  while retaining Picture Adjustment. The one-line fix passes
  `git diff --check`; it is unbuilt and uncommitted.

## TRY17 artifact verification

- Built `lineage-23.2-20260802-TRY17-UNOFFICIAL-odessa.zip` from the one-line
  LiveDisplay capability correction. It is 1,027,985,271 bytes with SHA-256
  `f76140e34c85f4877d78455abd78e00fb0a792e5f5ddb0f54a29191254d2c27f`.
  ZIP integrity passes and all required A/B OTA entries are present.
- Exact `payload.bin` is 1,027,977,977 bytes with SHA-256
  `8db2a18a89cc580e55fc212fae083c26fdf7dd72fb7b53603a9b46ec4af80855`;
  `FILE_SIZE` and base64 `FILE_HASH` in `payload_properties.txt` agree.
- Native `ota_extractor` completed all seven partitions: `boot`, `dtbo`,
  `product`, `recovery`, `system`, `vbmeta`, and `vendor`. Boot, DTBO, recovery,
  vbmeta, and vendor match target-files byte-for-byte. Product and system match
  after converting their target-files sparse images to raw images. Thus all
  seven exact payload outputs reproduce target-files.
- Exact payload/target-files recovery is 67,108,864 bytes with SHA-256
  `94a8cd76e6842bb46a4078c8db0e4f2259a6623c0fa48b5ac1fb52130b5d17e9`.
  The later loose `out/target/product/odessa/recovery.img` has SHA-256
  `dfb7790498e7f5260742b2441c7aa82f13e061bbb59b9b800eb443334a0bda39`
  and must not be mistaken for the exact OTA recovery.
- Exact payload vendor contains
  `vendor.lineage.livedisplay-service.sdm-pa.xml`, declaring only
  `IPictureAdjustment/default`; there is no Display Modes service fragment.
  The packaged service binary is 32,088 bytes with SHA-256
  `e24696ffc8faf0e71545761c2dd0228e3e40dd0ba72447faeceb5a4ec9845bda`.
- Standalone verbose `check_target_files_vintf` exited zero and returned
  `COMPATIBLE` for kernel `4.14.357-openela-perf+`. TRY17 remains uninstalled and
  the source fix remains uncommitted.

## TRY17 installation and Android result

- With explicit approval, sideloaded exact TRY17 from Recovery A to inactive
  slot B. Recovery completed status 0. Update Engine applied all 2,265 payload
  operations, verified all seven installed partition hashes exactly, completed
  postinstall, and reported successful application.
- Immediate post-install GPT capture has valid primary/backup CRCs and the exact
  proven activation pattern: all B non-boot partitions `0x04`, `boot_b=0x3f`,
  all A non-boot partitions `0x00`, and `boot_a=0x3a`.
- Automatic exact TRY17 Recovery B booted directly with incremental
  `1785708079`, proving slot activation before any manual GPT action.
- The user then flashed stock `gpt.bin` in bootloader but did not run
  `fastboot set_active`. A subsequent capture showed every slot-bearing LUN
  byte-identical to the immediate post-OTA capture; slot B remained exact and
  active. The stock partition-table flash therefore did not invalidate those
  slot-bearing GPT attributes in this instance.
- TRY17 Android displayed the Lineage boot animation and then a black screen;
  USB ceased enumerating. The user returned to exact TRY17 Recovery B with the
  hardware keys without another GPT flash.
- Mounted metadata `ro,noload` and pulled the bounded diagnostic from
  `/metadata/vold/odessa-bootdiag-logcat`. The host copy is 749,568 bytes with
  SHA-256 `838033fa0754882a9869e9a6e5232c486fda9d1245069edebdc9178171561a19`.
- TRY17 removed the `IDisplayModes/default` wait exactly as intended. SystemServer
  instead waits for declared `IPictureAdjustment/default` at
  `LineageHardwareManager.java:307`; servicemanager cannot start/register it,
  and watchdog kills SystemServer after the display thread blocks for 60 seconds.
  Thus this device's generic SDM backend supports neither advertised interface.
- Removed `vendor.lineage.livedisplay-service.sdm` from common product packages
  instead of compiling a daemon with both features disabled. This cleanly removes
  the binary, RC, and both device VINTF declarations. The TRY18 source candidate
  is unbuilt and uncommitted.

## TRY18 artifact verification

- Built `lineage-23.2-20260802-TRY18-UNOFFICIAL-odessa.zip`; size
  1,027,921,315 bytes, SHA-256
  `7bb7d5681027b869aa4f65b91681986677d3fa03e07c81e71972f17cef9eadba`.
  ZIP integrity passes and all required A/B OTA entries are present.
- Exact `payload.bin` is 1,027,914,021 bytes with SHA-256
  `5c9752da7f331ef55c7d29ec7e53ebaf3fb8a642f58bed9a353c03964db085b6`;
  recomputed base64 SHA-256 and size exactly match `payload_properties.txt`.
- Native extraction completed all seven payload partitions. Boot, DTBO,
  recovery, vbmeta, and vendor match target-files byte-for-byte; product and
  system match after converting target-files sparse images to raw. Exact payload
  recovery is 67,108,864 bytes with SHA-256
  `a03c5601c840a011a84d6d73cf5297ad8fb32b0feda2eafeebce96b3ef461ff9`.
- The later loose `out/target/product/odessa/recovery.img` has SHA-256
  `d0bf7672991f2aa152ea118a2106ca798781fab2da51b58f0eba1b7401a5e0c8`
  and is not the exact OTA recovery; do not use it for this test.
- Exact payload vendor contains no
  `vendor.lineage.livedisplay-service.sdm` binary, init RC, or device VINTF
  fragment. The framework compatibility matrix still names optional LiveDisplay
  interfaces, which is expected and does not declare a device service.
- Standalone `check_target_files_vintf` exited zero. TRY18 remains uninstalled;
  the source changes remain uncommitted.

## TRY18 installation and first full boot

- With explicit approval, sideloaded exact TRY18 from TRY17 Recovery B to slot A.
  Recovery completed status 0. Update Engine targeted A, applied all operations,
  verified all seven installed partition hashes exactly, completed postinstall,
  and reported successful application.
- Immediate post-OTA GPT was CRC-valid and exact: all A non-boot partitions
  `0x04`, `boot_a=0x3f`, all B non-boot partitions `0x00`, `boot_b=0x3a`.
- The first `adb reboot recovery` unexpectedly returned to TRY17 Recovery B;
  post-fallback GPT showed `boot_a=0x80` unbootable and slot B restored active.
  Offline unpacking proved TRY18 Recovery's kernel, DTB, recovery DTBO, command
  line, layout, and AVB descriptors valid; its ramdisk differs from proven TRY17
  only in build timestamp/incremental properties.
- With separate explicit approval, `fastboot set_active a` cleared the unbootable
  state. The already-installed exact TRY18 Recovery A then booted successfully
  with incremental `1785709822`. Its GPT normalized to the OTA pattern again.
  The initial fallback was therefore slot-state behavior, not a bad image.
- Normal TRY18 Android boot reached the visible **Welcome to LineageOS** setup
  wizard. This is the project's first successful full framework/UI boot and
  proves removing the unsupported generic SDM LiveDisplay service resolves the
  prior SystemServer watchdog loop.
- New blocker: touchscreen input is nonfunctional in both TRY18 Android and
  Lineage Recovery. This places the failure below SetupWizard in the shared
  kernel touch path. Odessa DTS has Novatek NT36xxx and Focaltech FT8756 variants
  on the same SPI chip-select; both modules are built and init explicitly loads
  them. Runtime recovery evidence is needed to identify the installed panel and
  determine whether probe, module load, GPIO/regulator, or firmware failed.

## TRY18 touchscreen runtime evidence

- Exact TRY18 Recovery A confirms touch is also nonfunctional there. Registered
  inputs contain only `qpnp_pon` and `gpio-keys`; no touchscreen input exists.
  `/proc/modules` is empty, no touch driver appears under `/sys/bus/spi/drivers`,
  `/sys/class/touchscreen` is absent, and dmesg reports
  `Unable to open /lib/modules, skipping module loading.`
- Recovery root has an empty `/vendor`, so the existing vendor-only modules,
  firmware, and `init.mmi.overlay.rc` cannot be used there. Build output proves
  `sensors_class.ko`, `nova_0flash_mmi.ko`, and `focaltech_0flash_mmi.ko` exist
  only in vendor; both touch modules depend on `sensors_class.ko`.
- Runtime SPI modalias identifies this exact XT2087-1 panel as Novatek:
  `OF_FULLNAME=/soc/spi@0xa80000/novatek@0`, compatible
  `novatek,NVT-ts-spi`, modalias `spi:NVT-ts-spi`. Odessa supports a separate
  Focaltech variant, so Recovery must retain both drivers for regional hardware.
- Added all three modules to `RECOVERY_KERNEL_MODULES` and their ordered load
  list to `BOARD_RECOVERY_KERNEL_MODULES_LOAD`. Added Novatek and Focaltech
  firmware to Recovery's `/vendor/firmware`, which is in Recovery ueventd's
  configured firmware search path. This is an unbuilt TRY19 candidate.
- Android's vendor image already contains the modules, dependency metadata,
  firmware, and explicit early-init modprobe command. Do not alter that path
  without Android runtime evidence; first use TRY19 Recovery to verify module
  loading and Novatek probe/firmware behavior directly.

## TRY19 artifact verification

- Built `lineage-23.2-20260802-TRY19-UNOFFICIAL-odessa.zip`; size
  1,028,303,013 bytes, SHA-256
  `65159bc3ece4727cbba2dc59659692d8a59b2e9ab530d1bf96fef96cb34a83d0`.
  ZIP integrity passes and all required A/B OTA entries are present.
- Exact `payload.bin` is 1,028,295,719 bytes with SHA-256
  `8b27737207a0221f30652e1ca158951c851173a376cdbd00e8e8747941355f35`;
  recomputed base64 SHA-256 and size match `payload_properties.txt` exactly.
- Native extraction completed all seven payload partitions. Boot, DTBO,
  recovery, vbmeta, and vendor match target-files byte-for-byte; product and
  system match after target sparse-to-raw conversion. Exact payload recovery is
  67,108,864 bytes with SHA-256
  `fa8b3d9684dadf6626254c02a01ab746dccd70e899d79f32b4a215bf6f479077`.
- The later loose `out/target/product/odessa/recovery.img` differs (SHA-256
  `aef4b20bdfd8c18abae8516b26a3a1aee7d5b95679936c5ad7fae4398fd7c56c`)
  and must not be used for the exact TRY19 test.
- Unpacked exact payload recovery contains `modules.load` in dependency order:
  `sensors_class`, `nova_0flash_mmi`, `focaltech_0flash_mmi`. `modules.dep`
  correctly records both touch drivers' `sensors_class.ko` dependency. All three
  modules and all four firmware files are present with the source/output hashes
  checked.
- Standalone `check_target_files_vintf` exited zero. TRY19 remains uninstalled;
  source changes remain uncommitted.

## TRY19 installation and Recovery regression

- With explicit approval, sideloaded exact TRY19 from TRY18 Recovery A to slot B.
  Recovery status 0; Update Engine targeted B, verified all seven installed
  partition hashes exactly, completed postinstall, and reported success.
- Immediate post-OTA GPT CRCs and attributes were exact: B non-boot `0x04`,
  `boot_b=0x3f`, A non-boot `0x00`, and `boot_a=0x3a`.
- Requesting Recovery B returned directly to bootloader. After the user flashed
  stock `gpt.bin`, bootloader still reported B selected and bootable with retry
  count 7, but `fastboot reboot recovery` again returned to bootloader.
- `fastboot boot` of exact payload recovery
  `fa8b3d9684dadf6626254c02a01ab746dccd70e899d79f32b4a215bf6f479077`
  also returned to bootloader. This bypasses GPT recovery selection and isolates
  the TRY19 recovery image. Its only functional change is automatic loading of
  the touch module set, so an early module load is the leading cause.
- Changed the next diagnostic to package all three modules and all firmware but
  place only `sensors_class` in `modules.load`. If that Recovery boots, insert
  `nova_0flash_mmi.ko` manually under ADB and capture its immediate probe/crash.
- Discovered all TRY16/TRY17/TRY18/TRY19 filenames and
  `lineage_odessa-ota.zip` share inode `38267682` (link count 8); all now hash as
  TRY19 (`65159bc3...`). The attempt to re-extract known-good TRY18 therefore
  produced TRY19 recovery. Device fastboot does not support read-only `fetch`, so
  slot-A recovery could not be copied back. Preserve future artifacts using a
  real copy/reflink with a distinct inode, then hash again.

## TRY20 sensors-class-only diagnostic

- Built `lineage-23.2-20260803-TRY20-UNOFFICIAL-odessa.zip`. It initially shared
  the mutable output inode, so it was immediately reflink-copied and atomically
  replaced. Preserved TRY20 now has distinct inode `38938410`, link count 1.
- Preserved OTA size is 1,028,302,945 bytes; SHA-256
  `5b17efc35a42d36b453300ca070998c5859e2fb8d7ef59b074428d571f2d5f83`.
  Exact payload is 1,028,295,651 bytes with SHA-256
  `d7cc086b534ae394a47e88dd94d7154544c612adcea7d02ad5a7ec3eb2362543`.
  Exact payload recovery SHA-256 is
  `0a99da4ba6ec263a0d55ee52b358aa15bb985f573c9a2dfc796fc5839856a993`.
- ZIP integrity, payload properties, all seven target partition comparisons,
  VINTF, modules, dependency metadata, and firmware passed. Exact Recovery keeps
  all three modules but `modules.load` contains only `sensors_class`.
- `fastboot boot` accepted the exact TRY20 recovery, but it returned to bootloader
  before ADB. Therefore automatic Novatek/Focaltech loading is not required for
  the regression. The next isolation should use an empty module load list while
  retaining the files, then RAM-boot; do not sideload another OTA first.
- Standalone report:
  `docs/try19-recovery-pre-adb-regression-20260803.md`.

## TRY21 empty-load-list candidate

- Changed only Odessa `BoardConfig.mk`: retained `sensors_class.ko`,
  `nova_0flash_mmi.ko`, and `focaltech_0flash_mmi.ko` in Recovery, but made
  `BOARD_RECOVERY_KERNEL_MODULES_LOAD` explicitly empty. Lineage's kernel-module
  helper will generate a zero-byte `/lib/modules/modules.load`; first-stage init
  safely iterates no modules. This isolates TRY20's `sensors_class` insertion
  from module packaging and ramdisk layout. No kernel, DT, Android module-load,
  or firmware source changed.
- `file(1)` initially classified `novatek_ts_fw.bin` and `novatek_ts_mp.bin` as
  aria2 control files because they are byte-identical 139,264-byte blobs with
  SHA-256
  `d9d1f5e88dc0fa90fdd64437e39adac7bf72ad70a8335e339cdb325cec2dab38`.
  TRY22 hardware later disproved the conclusion that they were invalid: the
  driver parsed the runtime blob and downloaded it successfully. Treat the
  identical runtime/MP payloads as a provenance oddity, not a proven blocker.
- Built `lineage-23.2-20260803-TRY21-UNOFFICIAL-odessa.zip`: 1,028,303,085
  bytes, current SHA-256
  `87d99911a9b2696238994b05ae27386b5ad3e556cfa6a5830b46bb510deccdfd`.
  ZIP integrity passes. Exact payload is 1,028,295,791 bytes, SHA-256
  `81c3c738e7b789e7bb75b5dd69c557af46457f2e1d36c61544e812ccda1520b1`;
  its size and base64 hash match `payload_properties.txt`.
- Native extraction of Recovery completed. Exact payload Recovery and target-files
  Recovery match byte-for-byte at SHA-256
  `ff352b4f294a8c902c118a954365ffd32ff0767455bd54f24b7abbd824402aac`.
  The loose `recovery.img` differs and must not be tested. Exact Recovery has a
  16,734,755-byte compressed ramdisk; extracted `modules.load` is zero bytes,
  all three module hashes remain identical to TRY20, and `modules.dep` retains
  both touch dependencies on `sensors_class`. Standalone VINTF checking exits 0.
- The named TRY21 ZIP again shares mutable inode `38267682` with
  `lineage_odessa-ota.zip` (link count 9), so it is not a preserved artifact and
  must not survive another build by filename alone.
- Exact TRY21 Recovery RAM-boots successfully and reaches ADB; touch remains
  unavailable as expected because `modules.load` is empty. This proves packaged
  `/lib/modules`, firmware, and ramdisk growth do not cause the TRY19/20 return.
- Manual `insmod /lib/modules/sensors_class.ko` returns 1 with
  `Required key not available`. Kernel dmesg reports
  `PKCS#7 signature not signed with a trusted key`; SELinux is permissive and is
  not the blocker. The runtime `.builtin_trusted_keys` has a different generated
  key from the current KERNEL_OBJ certificate used to sign the module.
- Root cause: `certs/system_certificates.S` embeds `signing_key.x509` with
  `.incbin`, but the compiler dependency/cache key did not include those bytes.
  `/home/arthu/bin/sccache-android clang` restored an older
  `system_certificates.o`; a fresh mtime did not imply fresh certificate bytes.
  Added the generated certificate SHA-256 to `AFLAGS_system_certificates.o` in
  kernel `certs/Makefile`, forcing Kbuild and sccache command keys to follow the
  actual certificate. Forced module-signature verification remains enabled.
- TRY22 runtime key changed to current certificate SKI `64bb5497...`.
  Manual `sensors_class.ko` insertion then succeeded, followed by successful
  `nova_0flash_mmi.ko` insertion and probe. The driver identified the NVT IC,
  registered IRQ 249 and input `event2`, created `/sys/class/touchscreen/NVT-ts`,
  parsed `novatek_ts_fw.bin` into 15 partitions, downloaded it in 84,049 us, and
  read firmware version 3 / PID `601F`. Recovery UI touchscreen input then
  worked normally, hardware-confirming the complete module/probe/firmware/input
  path. Rebooting lost this state because TRY22 intentionally had an empty
  `modules.load`; the installed older Recovery remained bootable but had no touch.
- Restored dependency-ordered `BOARD_RECOVERY_KERNEL_MODULES_LOAD` entries for
  `sensors_class.ko`, `nova_0flash_mmi.ko`, and `focaltech_0flash_mmi.ko` as the
  TRY23 candidate. This retains both regional panel variants. Android already
  requests `sensors_class` and both touch drivers from its vendor init scripts,
  so the corrected kernel certificate should also resolve Android touch without
  additional commands.
- Built and artifact-verified
  `lineage-23.2-20260804-TRY23-UNOFFICIAL-odessa.zip`; SHA-256 at verification was
  `7165120cfa144730deafe104432b03c47c222ca75c5f575bcca7650e00746309`.
  Exact payload Recovery SHA-256 was
  `2b80df2a644340dc9391b7237b849b36ea6f4daa793c8763591a5956f7f6a171`;
  native extraction matched target-files, VINTF passed, the corrected TRY22
  kernel remained unchanged, and `modules.load` contained `sensors_class`,
  Novatek, then Focaltech. The named ZIP remained a mutable hardlink.
- Exact TRY23 Recovery RAM-booted with touchscreen working automatically. The
  user then installed the TRY23 update and reports that it also works, completing
  hardware validation of the no-command touch fix.

## TRY23 initial hardware report

- User reports Wi-Fi and Bluetooth do not work. The imported Xiaomi SM6150
  networking changes used by the BPF backport are a hypothesis only; no logs yet
  connect either failure to that import. Tracked separately as `ODESSA-001` and
  `ODESSA-002`.
- Fingerprint enrollment/options are absent from Settings; sensor detection is
  not yet known. Tracked as `ODESSA-003`.
- Initial boot after installation still requires the user to flash the stock
  partition table manually. This means the automatic post-install boot path is
  not fixed despite successful payload installation. Tracked as P1
  `ODESSA-004`; do not request another GPT write without explicit permission and
  pre/post evidence.
- GPS remains untested. Vibration works, screen sleep works, and touchscreen
  works on installed TRY23.
- scrcpy audio capture throws
  `java.lang.UnsupportedOperationException: Cannot create AudioRecord`. This does
  not establish that all physical audio paths fail; mark audio partial until
  local playback, recording, calls, and the HAL are tested. Tracked as
  `ODESSA-005`.
- Front camera preview/capture, the primary rear camera, and camera flash work on
  installed TRY23. Additional rear cameras are missing from the camera interface;
  tracked as `ODESSA-006`. Video recording remains untested.
