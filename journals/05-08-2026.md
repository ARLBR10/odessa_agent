# 2026-08-05

## Camera provider enumeration

- The installed baseline is TRY25: `lineage-23.2-20260805-TRY25-UNOFFICIAL-odessa.zip`, build timestamp `1785888497`, OTA SHA-256 `abbc875d67411b2b197b24392ec8ee939f4bf07c67d058c780090d3d8ce9b1ca`.
- The device was queried over ADB only. No partition was flashed, erased, formatted, or otherwise written by the agent.
- `adb shell dumpsys media.camera` reports 8 total provider devices, 6 normal camera devices, and 6 public API1 devices. The API1 mappings are IDs `0`, `1`, `3`, `4`, `6`, and `7`.
- The provider characteristics identify ID `0` as the rear main sensor, ID `1` as front, ID `2` as the rear depth sensor, ID `3` as a rear sensor marked `is_ultrawide_action=1`, and ID `4` as a rear sensor marked `is_macro_action=1`. ID `5` is a logical depth camera with physical IDs `0` and `2`; IDs `6` and `7` are additional back-facing provider devices.
- Current runtime properties are `persist.vendor.camera.expose.aux=1` and `persist.vendor.camera.physical.num=5`. `vendor.camera.aux.packagelist`, `vendor.camera.aux.packageexcludelist`, and `persist.vendor.camera.privapp.list` are unset.
- Source review found Aperture's base `config_enableAuxCameras=false` in `packages/apps/Aperture/app/src/main/res/values/config.xml`, and no Odessa-specific overlay for that resource. `CameraRepository` always keeps IDs `0` and `1`, but rejects auxiliary IDs unless that boolean is enabled. Therefore the current evidence points to an Aperture resource gate, not missing HAL enumeration.
- Added an Odessa-owned Aperture resource overlay at `device/motorola/odessa/overlay-lineage/packages/apps/Aperture/app/src/main/res/values/config.xml` that sets only `config_enableAuxCameras=true`. No blob, HAL property, framework code, camera-ID exclusion, or logical-camera policy was changed.
- The first user-run `m Aperture` did not consume the new overlay: the resulting `product/app/Aperture/Aperture.apk` retained its 2026-07-29 timestamp and SHA-256 `850cfc34570b14e382b6a9aac96ebb722b11305e7918a9e1783f9a99302a62b6`. Odessa enforces RROs globally, and the new overlay directory was not represented in the existing build graph. Do not install that unchanged artifact.
- Added the Odessa Aperture overlay path to `PRODUCT_ENFORCE_RRO_EXCLUDED_OVERLAYS`, making this app overlay static and changing `device.mk` so the next user-run build regenerates the Make graph. `xmllint --noout` and `git diff --check` pass. The next checkpoint remains an APK-only build/install and hardware test of rear IDs `3`, `4`, `6`, and `7`; do not add guessed exclusions before those previews and captures are tested.
- The second user-run `m -j8 Aperture` completed successfully. The rebuilt `product/app/Aperture/Aperture.apk` is 8,079,423 bytes, has SHA-256 `c2403a3478c2a51198d5dec633c391baa40057e5927d910f4f59c553f9d46132`, and has host modification time `2026-08-05 17:30:36 -0300`. `aapt2 dump resources` confirms the compiled default value of `bool/config_enableAuxCameras` is `true`, while `config_ignoreLogicalAuxCameras` remains `true`.
- The user installed that APK as a userdata update to the system Aperture package and confirmed the auxiliary selectors work. The unfiltered rear selector showed two `0.9` entries (one monochrome and one color), three `1.0` entries from the same main lens with different processing/zoom, and one working `0.6` entry.
- Provider metadata resolves the useful and duplicate endpoints: ID `0` is physical `mot_s5kgw1` main (5.53 mm, f/1.8); ID `2` is physical `mot_ov02b1b` depth (1.94 mm, f/2.4); ID `3` is physical `mot_s5k4h7` with `is_ultrawide_action=1` (1.37 mm, f/2.2); ID `4` is physical `mot_ov02b10` with `is_macro_action=1` (1.94 mm, f/2.4); ID `5` is the already-filtered logical depth endpoint; ID `6` is `mot_zoom`; and ID `7` is `mfnr_zoom`. IDs `6` and `7` both report the main camera's 5.53 mm, f/1.8 characteristics.
- Refined the Aperture overlay to ignore IDs `2`, `6`, and `7`, retaining main ID `0`, ultrawide ID `3`, and macro ID `4`. This refinement is not built or hardware-tested yet.
- The user rebuilt the refined Aperture successfully. The APK is 8,079,423 bytes with SHA-256 `6367d7a78d20f171563732272445022f645730a2b515d2b0ad1b9514ecb4cc39`; `aapt2 dump resources` confirms `config_ignoredAuxCameraIds` is exactly `["2", "6", "7"]`. Hardware verification of the refined selector remains pending.
